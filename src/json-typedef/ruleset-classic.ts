import { toJtdForm } from 'json-typedef/utils'
import { mapValues } from 'lodash'
import * as RulesetClassic from 'schema/ruleset-classic'

// const jtd = mapValues(RulesetClassic, (v, k) => toJtdForm(v as any))

// export default jtd

// console.log(jtd)
