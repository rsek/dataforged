{
  "type": "object",
  "properties": {
    "Themes": {
      "type": "array",
      "items": { "$ref": "#/definitions/IDelveTheme" },
      "title": "Themes"
    },
    "Domains": {
      "type": "array",
      "items": { "$ref": "#/definitions/IDelveDomain" },
      "title": "Domains"
    }
  },
  "defaultProperties": [],
  "additionalProperties": false,
  "definitions": {
    "IDelveTheme": {
      "title": "IDelveTheme",
      "type": "object",
      "properties": {
        "Features": {
          "type": "array",
          "items": [
            { "$ref": "#/definitions/IDelveRow<1,4>" },
            { "$ref": "#/definitions/IDelveRow<5,8>" },
            { "$ref": "#/definitions/IDelveRow<9,12>" },
            { "$ref": "#/definitions/IDelveRow<13,16>" },
            { "$ref": "#/definitions/IDelveRow<17,20>" }
          ],
          "minItems": 5,
          "maxItems": 5,
          "title": "Features"
        },
        "Dangers": {
          "type": "array",
          "items": [
            { "$ref": "#/definitions/IDelveRow<1,5>" },
            { "$ref": "#/definitions/IDelveRow<6,10>" },
            { "$ref": "#/definitions/IDelveRow<11,12>" },
            { "$ref": "#/definitions/IDelveRow<13,14>" },
            { "$ref": "#/definitions/IDelveRow<15,16>" },
            { "$ref": "#/definitions/IDelveRow<17,18>" },
            { "$ref": "#/definitions/IDelveRow<19,20>" },
            { "$ref": "#/definitions/IDelveRow<21,22>" },
            { "$ref": "#/definitions/IDelveRow<23,24>" },
            { "$ref": "#/definitions/IDelveRow<35,26>" },
            { "$ref": "#/definitions/IDelveRow<27,28>" },
            { "$ref": "#/definitions/IDelveRow<29,30>" }
          ],
          "minItems": 12,
          "maxItems": 12,
          "title": "Dangers"
        },
        "Name": {
          "description": "The item's internal name. Should be unique among its sibling elements, as this key is often used (along with the object's ancestors) to generate its $id.\n\nIf the item has Display.Title, that should be preferred for most user-facing labels.",
          "type": "string",
          "title": "Name"
        },
        "Source": {
          "$ref": "#/definitions/ISource",
          "description": "Information on this item's source.",
          "title": "Source"
        },
        "Summary": {
          "description": "A user-facing markdown summary of the item.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Description": {
          "description": "A user-facing markdown description of the item, consisting of one or more paragraphs.",
          "type": "string",
          "title": "Description"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": [
        "Dangers",
        "Description",
        "Features",
        "Name",
        "Source",
        "Summary"
      ]
    },
    "IDelveRow<1,4>": {
      "title": "IDelveRow<1,4>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F": { "title": "IDelveRow.F", "type": "number", "enum": [1] },
    "IDelveRow.C": { "title": "IDelveRow.C", "type": "number", "enum": [4] },
    "IRow": {
      "description": "Interface representing a single row in an oracle table.",
      "title": "IRow",
      "type": "object",
      "properties": {
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Floor": {
          "anyOf": [
            {
              "description": "The low end of the dice range for this row.",
              "minimum": 1,
              "maximum": 100,
              "type": ["null", "integer"]
            },
            { "type": "null" }
          ],
          "title": "Floor"
        },
        "Ceiling": {
          "anyOf": [
            {
              "description": "The high end of the dice range for this row.",
              "minimum": 1,
              "maximum": 100,
              "type": ["null", "integer"]
            },
            { "type": "null" }
          ],
          "title": "Ceiling"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IMultipleRolls": {
      "description": "Describes{@linkIRow}results that call for multiple rolls, most commonly \"Roll twice\" results.",
      "title": "IMultipleRolls",
      "type": "object",
      "properties": {
        "Amount": {
          "description": "The number of rolls to make on the parent oracle table.",
          "type": "integer",
          "title": "Amount"
        },
        "Allow duplicates": {
          "description": "Whether to allow duplicate results when generating multiple rolls.\n\nImplicitly required by `Make it worse`.",
          "type": "boolean",
          "title": "Allow duplicates"
        },
        "Make it worse": {
          "description": "Whether duplicate rolls should be compounded in an Ironsworn-style \"Make it worse\" results.\n\nTypically this is accompanied by `IRow.Result` text like \"Roll twice more on this table. Both results occur. If they are the same result, make it worse.\"\n\nCan safely be ignored in Starforged-only implementations. Implicitly requires `Allow duplicates`.",
          "type": "boolean",
          "title": "Make it worse"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Allow duplicates", "Amount", "Make it worse"]
    },
    "IAttribute": {
      "description": "Describes an attribute key/value pair, set by an oracle row. The key-value pair should be set on any game object for which that row is generated.\n\nAttributes exist to describe prerequisites that might be fulfilled by more than one table, that don't exist on tables at all, or that a generated game object might want to 'force' as one of it's roll results.\n\nSee documentation for a list of available values.",
      "title": "IAttribute",
      "type": "object",
      "properties": {
        "Key": { "$ref": "#/definitions/AttributeKey", "title": "Key" },
        "Value": { "type": "string", "title": "Value" }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Key"]
    },
    "AttributeKey": {
      "title": "AttributeKey",
      "enum": [
        "Atmosphere",
        "Authority",
        "Creature Scale",
        "Derelict Type",
        "Disposition",
        "Dominion",
        "Encountered Behavior",
        "Environment",
        "Faction Type",
        "Fringe Group",
        "Guild",
        "Influence",
        "Initial Contact",
        "Leadership",
        "Life",
        "Location",
        "Location Theme",
        "Planetary Class",
        "Population",
        "Region",
        "Role",
        "Zone"
      ],
      "type": "string"
    },
    "IRollTemplate": {
      "description": "Describes the string keys of this item that should be replaced with template strings and filled with the results of one or more oracles.",
      "title": "IRollTemplate",
      "$ref": "#/definitions/IRollTemplate_1"
    },
    "IRollTemplate_1": {
      "description": "Describes the string keys of this item that should be replaced with template strings and filled with the results of one or more oracles.",
      "title": "IRollTemplate_1",
      "type": "object",
      "properties": {
        "Result": {
          "description": "A template string for the parent's `Result` property, to be filled with an oracle table roll Result.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A template string for the parent's `Summary` property, to be filled with an oracle table roll Result.",
          "type": "string",
          "title": "Summary"
        },
        "Description": {
          "description": "A template string for the parent's `Description` property, to be filled with an oracle table roll Result.",
          "type": "string",
          "title": "Description"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false
    },
    "ISuggestions": {
      "description": "Describes \"non-canonical\" suggestions for game content related to the parent item.\n\nThese are intended be offered as convenient shortcuts for the user (for instance, including a menu dropdown for rolling on suggested tables); having them roll automatically is **not recommended** for most projects.\n\nThese can be safely ignored if that functionality is not desired.",
      "title": "ISuggestions",
      "$ref": "#/definitions/ISuggestions_1"
    },
    "ISuggestions_1": {
      "description": "Describes \"non-canonical\" suggestions for game content related to the parent item.\n\nThese are intended be offered as convenient shortcuts for the user (for instance, including a menu dropdown for rolling on suggested tables); having them roll automatically is **not recommended** for most projects.\n\nThese can be safely ignored if that functionality is not desired.",
      "title": "ISuggestions_1",
      "type": "object",
      "properties": {
        "Game objects": {
          "description": "Suggested game objects and their parameters.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Oracle rolls": {
          "description": "Suggested oracle rolls, by table ID. Multiples of the same ID can be used to indicate that multiple rolls should be made.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Moves": {
          "description": "Suggested move IDs.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Moves"
        },
        "Assets": {
          "description": "Suggested asset IDs.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Assets"
        },
        "Encounters": {
          "description": "Suggested encounter IDs.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Encounters"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false
    },
    "IGameObject": {
      "description": "Describes a game object, with optional required parameters (for example, a specific Location result).",
      "title": "IGameObject",
      "type": "object",
      "properties": {
        "Object type": {
          "$ref": "#/definitions/GameObjectType",
          "title": "Object type"
        },
        "Requires": {
          "$ref": "#/definitions/IRequirements",
          "title": "Requires"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Object type"]
    },
    "GameObjectType": {
      "title": "GameObjectType",
      "enum": [
        "Character",
        "Creature",
        "Derelict",
        "Derelict Zone",
        "Faction",
        "Planet",
        "Precursor Vault",
        "Settlement",
        "Starship"
      ],
      "type": "string"
    },
    "IRequirements": {
      "description": "Data describing an item's requirements.",
      "title": "IRequirements",
      "type": "object",
      "properties": {
        "Attributes": {
          "description": "A list of attribute keys, and values of those keys that satisfy the requirements.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttributeChoices" },
          "title": "Attributes"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Attributes"]
    },
    "IAttributeChoices": {
      "title": "IAttributeChoices",
      "type": "object",
      "properties": {
        "Key": { "$ref": "#/definitions/AttributeKey", "title": "Key" },
        "Values": {
          "type": "array",
          "items": { "type": "string" },
          "title": "Values"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Key"]
    },
    "IOracleContent": {
      "description": "Interface for metadata that describes an oracle's semantic or lexical content.",
      "title": "IOracleContent",
      "$ref": "#/definitions/IOracleContent_1"
    },
    "IOracleContent_1": {
      "description": "Interface for metadata that describes an oracle's semantic or lexical content.",
      "title": "IOracleContent_1",
      "type": "object",
      "properties": {
        "Part of speech": {
          "description": "The part of speech of this oracle.",
          "type": "array",
          "items": {
            "enum": [
              "adjective",
              "common noun",
              "compound noun",
              "fragment",
              "name",
              "noun",
              "plural",
              "possessive case",
              "proper noun",
              "proper noun fragment",
              "sentences",
              "verb"
            ],
            "type": "string"
          },
          "title": "Part of speech"
        },
        "Tags": {
          "description": "Any arbitrary string tags associated with this oracle.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Tags"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false
    },
    "IDisplay": {
      "description": "Interface for data relevant to an item's display/rendering.",
      "title": "IDisplay",
      "$ref": "#/definitions/IDisplay_1"
    },
    "IDisplay_1": {
      "description": "Interface for data relevant to an item's display/rendering.",
      "title": "IDisplay_1",
      "type": "object",
      "properties": {
        "Icon": {
          "description": "A URL pointing to a single SVG icon.",
          "pattern": "^\\.\\./\\.\\./img/vector/[A-z-_0-9/]+\\.svg$",
          "type": "string",
          "title": "Icon"
        },
        "Images": {
          "description": "An array of URLs pointing to one or more WEBP images.",
          "pattern": "^\\.\\./\\.\\./img/raster/[A-z-_0-9/]+\\.webp$",
          "type": "array",
          "items": { "type": "string" },
          "title": "Images"
        },
        "Color": {
          "description": "A hex color associated with this item, for use as e.g. an accent color in its display.",
          "pattern": "^#[A-f0-9][A-f0-9][A-f0-9][A-f0-9][A-f0-9][A-f0-9]$",
          "type": "string",
          "title": "Color"
        },
        "Title": {
          "description": "The title of this item as it appears printed in the rulebook. Intended for use as the item's header, label, etc.",
          "type": "string",
          "title": "Title"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false
    },
    "IDelveRow<5,8>": {
      "title": "IDelveRow<5,8>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_1",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_1",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_1": {
      "title": "IDelveRow.F_1",
      "type": "number",
      "enum": [5]
    },
    "IDelveRow.C_1": {
      "title": "IDelveRow.C_1",
      "type": "number",
      "enum": [8]
    },
    "IDelveRow<9,12>": {
      "title": "IDelveRow<9,12>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_2",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_2",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_2": {
      "title": "IDelveRow.F_2",
      "type": "number",
      "enum": [9]
    },
    "IDelveRow.C_2": {
      "title": "IDelveRow.C_2",
      "type": "number",
      "enum": [12]
    },
    "IDelveRow<13,16>": {
      "title": "IDelveRow<13,16>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_3",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_3",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_3": {
      "title": "IDelveRow.F_3",
      "type": "number",
      "enum": [13]
    },
    "IDelveRow.C_3": {
      "title": "IDelveRow.C_3",
      "type": "number",
      "enum": [16]
    },
    "IDelveRow<17,20>": {
      "title": "IDelveRow<17,20>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_4",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_4",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_4": {
      "title": "IDelveRow.F_4",
      "type": "number",
      "enum": [17]
    },
    "IDelveRow.C_4": {
      "title": "IDelveRow.C_4",
      "type": "number",
      "enum": [20]
    },
    "IDelveRow<1,5>": {
      "title": "IDelveRow<1,5>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_5",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.C_5": {
      "title": "IDelveRow.C_5",
      "type": "number",
      "enum": [5]
    },
    "IDelveRow<6,10>": {
      "title": "IDelveRow<6,10>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_5",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_6",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_5": {
      "title": "IDelveRow.F_5",
      "type": "number",
      "enum": [6]
    },
    "IDelveRow.C_6": {
      "title": "IDelveRow.C_6",
      "type": "number",
      "enum": [10]
    },
    "IDelveRow<11,12>": {
      "title": "IDelveRow<11,12>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_6",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_2",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_6": {
      "title": "IDelveRow.F_6",
      "type": "number",
      "enum": [11]
    },
    "IDelveRow<13,14>": {
      "title": "IDelveRow<13,14>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_3",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_7",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.C_7": {
      "title": "IDelveRow.C_7",
      "type": "number",
      "enum": [14]
    },
    "IDelveRow<15,16>": {
      "title": "IDelveRow<15,16>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_7",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_3",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_7": {
      "title": "IDelveRow.F_7",
      "type": "number",
      "enum": [15]
    },
    "IDelveRow<17,18>": {
      "title": "IDelveRow<17,18>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_4",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_8",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.C_8": {
      "title": "IDelveRow.C_8",
      "type": "number",
      "enum": [18]
    },
    "IDelveRow<19,20>": {
      "title": "IDelveRow<19,20>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_8",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_4",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_8": {
      "title": "IDelveRow.F_8",
      "type": "number",
      "enum": [19]
    },
    "IDelveRow<21,22>": {
      "title": "IDelveRow<21,22>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_9",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_9",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_9": {
      "title": "IDelveRow.F_9",
      "type": "number",
      "enum": [21]
    },
    "IDelveRow.C_9": {
      "title": "IDelveRow.C_9",
      "type": "number",
      "enum": [22]
    },
    "IDelveRow<23,24>": {
      "title": "IDelveRow<23,24>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_10",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_10",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_10": {
      "title": "IDelveRow.F_10",
      "type": "number",
      "enum": [23]
    },
    "IDelveRow.C_10": {
      "title": "IDelveRow.C_10",
      "type": "number",
      "enum": [24]
    },
    "IDelveRow<35,26>": {
      "title": "IDelveRow<35,26>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_11",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_11",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_11": {
      "title": "IDelveRow.F_11",
      "type": "number",
      "enum": [35]
    },
    "IDelveRow.C_11": {
      "title": "IDelveRow.C_11",
      "type": "number",
      "enum": [26]
    },
    "IDelveRow<27,28>": {
      "title": "IDelveRow<27,28>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_12",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_12",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_12": {
      "title": "IDelveRow.F_12",
      "type": "number",
      "enum": [27]
    },
    "IDelveRow.C_12": {
      "title": "IDelveRow.C_12",
      "type": "number",
      "enum": [28]
    },
    "IDelveRow<29,30>": {
      "title": "IDelveRow<29,30>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_13",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_13",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_13": {
      "title": "IDelveRow.F_13",
      "type": "number",
      "enum": [29]
    },
    "IDelveRow.C_13": {
      "title": "IDelveRow.C_13",
      "type": "number",
      "enum": [30]
    },
    "ISource": {
      "description": "Interface representing data on the game's source.",
      "title": "ISource",
      "type": "object",
      "properties": {
        "Title": {
          "$ref": "#/definitions/SourceTitle",
          "description": "The title of the source.",
          "title": "Title"
        },
        "Date": {
          "description": "The 6-number date string formatted as `MMDDYY`. Relevant only during Starforged development; it will be deprecated once the game is released.",
          "pattern": "^(0[1-9]|1[0-2])([0-2][1-9]|3[0-1])([0-9][0-9])$",
          "type": "string",
          "title": "Date"
        },
        "Page": {
          "description": "The page on which the item appears most prominently.",
          "type": "integer",
          "title": "Page"
        },
        "Url": {
          "description": "The URL where the source is available.",
          "pattern": "^https?://.*$",
          "type": "string",
          "title": "Url"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Title"]
    },
    "SourceTitle": {
      "description": "Enumerates valid sourcebook titles.",
      "title": "SourceTitle",
      "enum": [
        "Custom",
        "Ironsworn Assets",
        "Ironsworn Bonus Assets (July 2020)",
        "Ironsworn Rulebook",
        "Ironsworn: Delve",
        "Ironsworn: Starforged Assets",
        "Ironsworn: Starforged Rulebook",
        "Sundered Isles Preview"
      ],
      "type": "string"
    },
    "IDelveDomain": {
      "title": "IDelveDomain",
      "type": "object",
      "properties": {
        "Features": {
          "type": "array",
          "items": [
            { "$ref": "#/definitions/IDelveRow<21,43>" },
            { "$ref": "#/definitions/IDelveRow<44,56>" },
            { "$ref": "#/definitions/IDelveRow<57,64>" },
            { "$ref": "#/definitions/IDelveRow<65,68>" },
            { "$ref": "#/definitions/IDelveRow<69,72>" },
            { "$ref": "#/definitions/IDelveRow<73,76>" },
            { "$ref": "#/definitions/IDelveRow<77,80>" },
            { "$ref": "#/definitions/IDelveRow<81,84>" },
            { "$ref": "#/definitions/IDelveRow<85,88>" },
            { "$ref": "#/definitions/IDelveRow<89,98>" },
            { "$ref": "#/definitions/IDelveRow<99,99>" },
            { "$ref": "#/definitions/IDelveRow<100,100>" }
          ],
          "minItems": 12,
          "maxItems": 12,
          "title": "Features"
        },
        "Dangers": {
          "type": "array",
          "items": [
            { "$ref": "#/definitions/IDelveRow<31,33>" },
            { "$ref": "#/definitions/IDelveRow<34,36>" },
            { "$ref": "#/definitions/IDelveRow<37,39>" },
            { "$ref": "#/definitions/IDelveRow<40,42>" },
            { "$ref": "#/definitions/IDelveRow<43,45>" }
          ],
          "minItems": 5,
          "maxItems": 5,
          "title": "Dangers"
        },
        "Name": {
          "description": "The item's internal name. Should be unique among its sibling elements, as this key is often used (along with the object's ancestors) to generate its $id.\n\nIf the item has Display.Title, that should be preferred for most user-facing labels.",
          "type": "string",
          "title": "Name"
        },
        "Source": {
          "$ref": "#/definitions/ISource",
          "description": "Information on this item's source.",
          "title": "Source"
        },
        "Summary": {
          "description": "A user-facing markdown summary of the item.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Description": {
          "description": "A user-facing markdown description of the item, consisting of one or more paragraphs.",
          "type": "string",
          "title": "Description"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": [
        "Dangers",
        "Description",
        "Features",
        "Name",
        "Source",
        "Summary"
      ]
    },
    "IDelveRow<21,43>": {
      "title": "IDelveRow<21,43>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_9",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_14",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.C_14": {
      "title": "IDelveRow.C_14",
      "type": "number",
      "enum": [43]
    },
    "IDelveRow<44,56>": {
      "title": "IDelveRow<44,56>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_14",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_15",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_14": {
      "title": "IDelveRow.F_14",
      "type": "number",
      "enum": [44]
    },
    "IDelveRow.C_15": {
      "title": "IDelveRow.C_15",
      "type": "number",
      "enum": [56]
    },
    "IDelveRow<57,64>": {
      "title": "IDelveRow<57,64>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_15",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_16",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_15": {
      "title": "IDelveRow.F_15",
      "type": "number",
      "enum": [57]
    },
    "IDelveRow.C_16": {
      "title": "IDelveRow.C_16",
      "type": "number",
      "enum": [64]
    },
    "IDelveRow<65,68>": {
      "title": "IDelveRow<65,68>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_16",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_17",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_16": {
      "title": "IDelveRow.F_16",
      "type": "number",
      "enum": [65]
    },
    "IDelveRow.C_17": {
      "title": "IDelveRow.C_17",
      "type": "number",
      "enum": [68]
    },
    "IDelveRow<69,72>": {
      "title": "IDelveRow<69,72>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_17",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_18",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_17": {
      "title": "IDelveRow.F_17",
      "type": "number",
      "enum": [69]
    },
    "IDelveRow.C_18": {
      "title": "IDelveRow.C_18",
      "type": "number",
      "enum": [72]
    },
    "IDelveRow<73,76>": {
      "title": "IDelveRow<73,76>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_18",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_19",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_18": {
      "title": "IDelveRow.F_18",
      "type": "number",
      "enum": [73]
    },
    "IDelveRow.C_19": {
      "title": "IDelveRow.C_19",
      "type": "number",
      "enum": [76]
    },
    "IDelveRow<77,80>": {
      "title": "IDelveRow<77,80>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_19",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_20",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_19": {
      "title": "IDelveRow.F_19",
      "type": "number",
      "enum": [77]
    },
    "IDelveRow.C_20": {
      "title": "IDelveRow.C_20",
      "type": "number",
      "enum": [80]
    },
    "IDelveRow<81,84>": {
      "title": "IDelveRow<81,84>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_20",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_21",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_20": {
      "title": "IDelveRow.F_20",
      "type": "number",
      "enum": [81]
    },
    "IDelveRow.C_21": {
      "title": "IDelveRow.C_21",
      "type": "number",
      "enum": [84]
    },
    "IDelveRow<85,88>": {
      "title": "IDelveRow<85,88>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_21",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_22",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_21": {
      "title": "IDelveRow.F_21",
      "type": "number",
      "enum": [85]
    },
    "IDelveRow.C_22": {
      "title": "IDelveRow.C_22",
      "type": "number",
      "enum": [88]
    },
    "IDelveRow<89,98>": {
      "title": "IDelveRow<89,98>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_22",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_23",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_22": {
      "title": "IDelveRow.F_22",
      "type": "number",
      "enum": [89]
    },
    "IDelveRow.C_23": {
      "title": "IDelveRow.C_23",
      "type": "number",
      "enum": [98]
    },
    "IDelveRow<99,99>": {
      "title": "IDelveRow<99,99>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_23",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_24",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_23": {
      "title": "IDelveRow.F_23",
      "type": "number",
      "enum": [99]
    },
    "IDelveRow.C_24": {
      "title": "IDelveRow.C_24",
      "type": "number",
      "enum": [99]
    },
    "IDelveRow<100,100>": {
      "title": "IDelveRow<100,100>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_24",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_25",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_24": {
      "title": "IDelveRow.F_24",
      "type": "number",
      "enum": [100]
    },
    "IDelveRow.C_25": {
      "title": "IDelveRow.C_25",
      "type": "number",
      "enum": [100]
    },
    "IDelveRow<31,33>": {
      "title": "IDelveRow<31,33>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_25",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_26",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_25": {
      "title": "IDelveRow.F_25",
      "type": "number",
      "enum": [31]
    },
    "IDelveRow.C_26": {
      "title": "IDelveRow.C_26",
      "type": "number",
      "enum": [33]
    },
    "IDelveRow<34,36>": {
      "title": "IDelveRow<34,36>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_26",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_27",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_26": {
      "title": "IDelveRow.F_26",
      "type": "number",
      "enum": [34]
    },
    "IDelveRow.C_27": {
      "title": "IDelveRow.C_27",
      "type": "number",
      "enum": [36]
    },
    "IDelveRow<37,39>": {
      "title": "IDelveRow<37,39>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_27",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_28",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_27": {
      "title": "IDelveRow.F_27",
      "type": "number",
      "enum": [37]
    },
    "IDelveRow.C_28": {
      "title": "IDelveRow.C_28",
      "type": "number",
      "enum": [39]
    },
    "IDelveRow<40,42>": {
      "title": "IDelveRow<40,42>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_28",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_29",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_28": {
      "title": "IDelveRow.F_28",
      "type": "number",
      "enum": [40]
    },
    "IDelveRow.C_29": {
      "title": "IDelveRow.C_29",
      "type": "number",
      "enum": [42]
    },
    "IDelveRow<43,45>": {
      "title": "IDelveRow<43,45>",
      "type": "object",
      "properties": {
        "Floor": {
          "$ref": "#/definitions/IDelveRow.F_29",
          "description": "The low end of the dice range for this row.",
          "title": "Floor"
        },
        "Ceiling": {
          "$ref": "#/definitions/IDelveRow.C_30",
          "description": "The high end of the dice range for this row.",
          "title": "Ceiling"
        },
        "$id": {
          "anyOf": [
            {
              "description": "The ID of this row.",
              "pattern": "^(Ironsworn|Starforged)/Oracles(/[A-z_-]+)+/[1-9][0-9]*(-[1-9][0-9]*)?(/Subtable/[1-9][0-9]*(-[1-9][0-9]*)?)?$",
              "type": ["null", "string"]
            },
            { "type": "null" }
          ],
          "title": "$id"
        },
        "Result": {
          "description": "The primary result text for the row, annotated in Markdown.\nIn the book, this is frequently the only column aside from the roll column. Otherwise, it is the first column.\nSome tables label this column as something other than Result; see the parent (or grandparent) Oracle.Display for more information.",
          "type": "string",
          "title": "Result"
        },
        "Summary": {
          "description": "A secondary markdown string that must be presented to the user for the implementation to be complete, but may benefit from progressive disclosure (such as a collapsible element, popover/tooltip, etc).\n\nSome tables label this column as something other than Result; see the parent (or grandparent) `IOracle.Display.Table` for more information.\n\n`null` is used in cases where an 'empty' `Summary` exists (example: Starship Type, p. 326). In the book, these table cells are rendered with the text `--` (and this is the recommended placeholder for tabular display). For display as a single result (e.g. VTT table roll output), however, `null` values can be safely omitted.",
          "type": ["null", "string"],
          "title": "Summary"
        },
        "Oracle rolls": {
          "description": "Additional oracle tables that should be rolled when this row is selected.",
          "type": "array",
          "items": { "type": "string" },
          "title": "Oracle rolls"
        },
        "Subtable": {
          "description": "A table to be rolled when this row is selected. If this row references an external oracle, the `Oracles` property is used instead.",
          "type": "array",
          "items": { "$ref": "#/definitions/IRow" },
          "title": "Subtable"
        },
        "Multiple rolls": {
          "description": "Data for rows that call for multiple rolls, e.g. on `Roll twice` results.",
          "$ref": "#/definitions/IMultipleRolls",
          "title": "Multiple rolls"
        },
        "Attributes": {
          "description": "The attributes set by this row.",
          "type": "array",
          "items": { "$ref": "#/definitions/IAttribute" },
          "title": "Attributes"
        },
        "Roll template": {
          "$ref": "#/definitions/IRollTemplate",
          "description": "Describes the string values of this item that should be replaced with template strings and filled with the results of one or more oracle rolls.",
          "title": "Roll template"
        },
        "Suggestions": {
          "$ref": "#/definitions/ISuggestions",
          "description": "\"Non-canonical\" suggestions of related items. They might be convenient to present to the user, but in most implementations rolling them automatically is not recommended.",
          "title": "Suggestions"
        },
        "Content": {
          "$ref": "#/definitions/IOracleContent",
          "description": "Metadata that describes an oracle's semantic or lexical content.",
          "title": "Content"
        },
        "Game objects": {
          "description": "Any game objects that are explicitly pointed to by the original text. For most implementations, it is *not* recommended to generate them automatically - see \"Peeling the Onion\", p. 293.",
          "type": "array",
          "items": { "$ref": "#/definitions/IGameObject" },
          "title": "Game objects"
        },
        "Display": {
          "$ref": "#/definitions/IDisplay",
          "description": "Data relevant to this item's display/rendering.",
          "title": "Display"
        }
      },
      "defaultProperties": [],
      "additionalProperties": false,
      "required": ["Ceiling", "Floor", "Result"]
    },
    "IDelveRow.F_29": {
      "title": "IDelveRow.F_29",
      "type": "number",
      "enum": [43]
    },
    "IDelveRow.C_30": {
      "title": "IDelveRow.C_30",
      "type": "number",
      "enum": [45]
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
